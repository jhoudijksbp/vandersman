type Item {
  id: ID!
  name: String
  description: String
}

input NewItemInput {
  id: ID!
  name: String
  description: String
}

type Query {
  getItem(id: ID!): Item
  listItems: [Item]
}

type Mutation {
  addItem(input: NewItemInput!): Item
}
